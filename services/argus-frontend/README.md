# argus-frontend

Canonical source location: [`/frontend`](../../frontend)

This mirror directory is provided to match the requested documentation-oriented structure without breaking existing Compose and Docker build paths.
