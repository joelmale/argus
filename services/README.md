# Services

This directory exists as a documentation-oriented service map for the Docusaurus layout.

Canonical source locations remain:

- scanner service: [`/scanner`](../scanner)
- backend service: [`/backend`](../backend)
- frontend service: [`/frontend`](../frontend)

The live build, Docker Compose files, and development workflows still use those canonical directories.
